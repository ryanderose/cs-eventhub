# Turbo remote caching credentials (local-only; do not commit real values)
# Copy to `.env.turbo` or `.env.turbo.local` and fill in your actual secrets.
TURBO_TEAM=team_8cAN0fZmUmokoMKTyciPlPOm
TURBO_TOKEN=replace-me-with-token

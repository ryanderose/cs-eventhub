# Public endpoints (local defaults)
NEXT_PUBLIC_API_BASE=http://localhost:4000
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_CONFIG_URL=http://localhost:4000/config/tenants/demo.json

# Optional: if the admin references the embed manifest
NEXT_PUBLIC_EMBED_MANIFEST=https://cdn.townthink.com/hub-embed@latest/manifest.json

# Telemetry
TELEMETRY_MODE=dev

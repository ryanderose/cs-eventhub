# Local adapter defaults
PORT=4000
HOST=0.0.0.0
TELEMETRY_MODE=dev

# Persistence (recommended)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Config signing (optional)
CONFIG_SIGNING_SECRET=

# CDN overrides (optional)
CONFIG_CDN_ORIGIN=
CONFIG_BETA_MANIFEST=
CONFIG_PROD_MANIFEST=
CONFIG_BETA_EMBED_SRC=
CONFIG_PROD_EMBED_SRC=
CONFIG_API_BASE=

# Caching / tuning (optional)
PLAN_INLINE_LIMIT=2000
PLAN_CACHE_TTL_SECONDS=3600
COMPOSER_VERSION=

# If any handlers run on Edge, keep secrets in Vercel encrypted envs

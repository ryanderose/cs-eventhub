# Hostname and port for the config service
CONFIG_HOST=config.localhost
CONFIG_PORT=4001

# Secret used to sign the tenant configuration payloads
CONFIG_SIGNING_SECRET=change-me

# Override upstream endpoints when mirroring production
# CONFIG_BETA_MANIFEST=http://cdn.localhost:5050/hub-embed@latest/manifest.json
# CONFIG_PROD_MANIFEST=https://cdn.townthink.com/hub-embed@latest/manifest.json
# CONFIG_API_BASE=https://api.townthink.com
